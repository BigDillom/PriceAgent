[task]
name = "barrier_up_and_out_pricing"
version = "1.0"
category = "derivatives"
difficulty = "easy"

[environment]
python = "3.10"
packages = ["derivkit", "numpy", "scipy", "pandas", "pydantic", "PyYAML", "numba"]

[output]
path = "/app/output"
format = "json"

[grading]
tolerance_pv = 0.01
fields = ["pv"]
